function ProgramCard({ program }) {
      return (
          <article>
                <h3>{program.name}</h3>

                      <p>{program.description}</p>
                          </article>
                            );
                            }

                            export default ProgramCard;
